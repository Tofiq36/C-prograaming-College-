#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    float c, f;
    scanf("%f", &c);
    f = ((c * 9) / 5) + 32;
    scanf("%f", &f);
    printf("%.2f", f);
    return 0;
}
