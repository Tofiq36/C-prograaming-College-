#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int A, B;
    scanf("%d%d", &A, &B);
    if (A > B)
        printf("%d", A);
    else if (B > A)
        printf("%d", B);
    else if (A == B)
        printf("%d", A);
    return 0;
}
