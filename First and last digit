#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>


int main() {
    int N;
    scanf("%d", &N);

    int num = N;
    if (num < 0) {
        num = -num;
    }
    int last_digit = num % 10;
    while (num >= 10) {
        num = num / 10;
    }
    int first_digit = num;
    printf("%d %d", first_digit, last_digit);

    return 0;
}
