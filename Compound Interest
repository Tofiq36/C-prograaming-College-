#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int P, T;
    float R;
    scanf("%d%f%d", &P, &R, &T);
    float amount = P * pow((1 + R/100), T);
    float CI = amount - P;
    printf("%.2f", CI);
    return 0;
}
